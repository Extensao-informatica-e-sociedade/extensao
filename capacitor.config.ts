import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.extensaoconectaturismo',
  appName: 'EmpreendeTech',
  webDir: 'dist',
};

export default config;
